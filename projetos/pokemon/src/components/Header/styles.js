import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    backgroundColor: "#efeff4",
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "3%",
    boxShadow: "0px 2px #bdb9b8",
  },

  title: {
    fontWeight: "bold",
    fontSize: "24px",
    fontFamily: "sans-serif",
  },
});
